import React from 'react';

function HeroSection() {
    return (
        <section className="hero">
            <h1>Welcome to International Student Job Portal</h1>
            <p>Find the best job opportunities for international students</p>
            <button>Find a Job</button>
        </section>
    );
}

export default HeroSection;
